/**
 * File: /src/routes/group
 * Date: 28/08/2023
 */
const express = require("express");
const authenticateToken = require("../middleware/authenticateToken");

const router = express.Router();

// PATH 1: (POST) /api/group/newgroup

module.exports = router;